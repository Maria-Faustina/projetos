import tkinter as tk
import random


simbolos = ["😒","🤑","😱","🐺","🫦","✨"]
saldo = 20.0
custo_giro = float(input(" Insira a aposta"))

def girar():
    global saldo
    if saldo < custo_giro:
        resultado_label.config(text="Saldo insulficiente",fg="red")
        return

saldo -= custo_giro

resultado_label = [random.choise(simbolos) or in range(3)]

slot1.config(text=resultado[0])
slot2.config(text=resultado[1])
slot3.config(text=resultado[3])

janela = tk.Tk()
janela.title(" Jogo da Oncinha")
janela.geometry("250x300")
janela.resizable(True,True)

titulo = tk.Label(janela, text=(" É o tigrrrrruuu8uu"))
titulo.pack(pady=10)

frame_slots = tk.Frame(janela)
frame_slots.pack(pady=10)

slot1 = tk.Label(frame_slots, text="⁉️",font=("Arial",30))
slot1.pack(side="left", padx=10)

slot2 = tk.Label(frame_slots, text="⁉️",font=("Arial",30))
slot2.pack(side="left", padx=10)

slot3 = tk.Label(frame_slots, text="⁉️",font=("Arial",30))
slot3.pack(side="left", padx=10)

resultado_label
saldo_Label = tk.Label(janela, text=f"Saldo: R$ {saldo:.2f}")

botao = tk.Button(janela, text=("Arial", 12), command=girar)
botao.pack
